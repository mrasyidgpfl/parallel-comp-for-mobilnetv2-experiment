package com.fasilkom.kotlin.sibipenerjemah.service.common

/** The runtime device type used for executing classification.  */
enum class Device {
    CPU, GPU
}

enum class LiteModelType {
    Reguler, RandomPixel
}